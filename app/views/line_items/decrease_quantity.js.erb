var count_item = $('#count_item_<%= @line_item.id %>');
var line_item = $('#line_<%= @line_item.id %>');

if(parseInt(count_item.html()) > 1){
  count_item.html(function(i, val) { return val*1-1 });
}
else{
  line_item.remove();
}
calculate_items();
