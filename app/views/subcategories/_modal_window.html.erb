<div class="modal fade product_view_<%= i+1 %>" id="product_view_<%= i+1 %>">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <a href="#" data-dismiss="modal" class="class pull-right"><span class="glyphicon glyphicon-remove"></span></a>
        <h2 class="modal-title">
          <%= product.title %>
        </h2>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="col-md-3">
            <% if product.category.parent.present? %>
              <p>
                <b>Категория:</b>
                <%= product.category.parent.name %>
              </p>
              <p>
                <b>Подкатегория:</b>
                <%= product.category.name %>
              </p>
            <% else %>
              <p>
                <b>Категория:</b>
                <%= product.category.name %>
              </p>
            <% end %>
            <p>
              <strong>Пол:</strong>
              <% if product.boys %>
                <%= fa_icon 'mars-stroke', class: 'fa-2x sex-boys', alt: 'Мальчики', title: "Для мальчиков" %>
              <% end %>
              <% if product.girls %>
                <%= fa_icon 'venus', class: 'fa-2x sex-girls', alt: 'Девочки', title: "Для девочек" %>
              <% end %>
            </p>
            <p>
              <strong>Бренд:</strong>
              <%= product.brand %>
            </p>
            <p>
              <strong>Страна:</strong>
              <%= product.country %>
            </p>
          </div>
          <div class="col-md-6" id="slider">
            <div class="col-sm-12" id="carousel-bounding-box">
              <div class="carousel slide carousel-product" id="myCarousel">
                <div class="carousel-inner">
                  <div class="active item" data-slide-number="0">
                    <% if product.image.present? %>
                      <img id="main_avatar" src="<%= product.image_url(:large) %>" data-zoom-image="<%= product.image_url(:large) %>", class='img-thumbnail profile-avatar'/>
                    <% else %>
                      <%= image_tag( product.image_url(:large), width: '470px', height: '480px') %>
                    <% end %>
                  </div>
                  <% product.image_products.each_with_index do |im_prod, i| %>
                    <div class="item" data-slide-number="<%= (i+1) %>"">
                      <img id="image_<%= im_prod.id %>" src="<%= im_prod.image.url(:large) %>" data-zoom-image="<%= im_prod.image.url(:large) %>", class='img-thumbnail profile-avatar', width='470px', height='480px'/>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
          <div class="col-md-3">
            <p><span class="uk-badge uk-badge-success">НОВИНКА!</span></p>
            <h2>
              <% if product.discount != 0 %>
                <s><%= number_to_currency(product.price, unit: "₴")%></s>
                <p class='label label-success'><%= number_to_currency(product.discount_price, unit: "₴")%></p> 
              <% else %>
                <%= number_to_currency(product.price, unit: "₴")%>
              <% end %>
            </h2>
            <% if product.discount %>
              <% if product.discount != 0 %>
                <p>
                  <span class="uk-badge uk-badge-notification uk-badge-danger">
                    <strong>Скидка</strong>
                    <%= number_to_percentage(product.discount, precision: 0) %>
                  </span>
                </p>
              <% end %>
            <% end %>
            <p>
              <strong>Осталось на складе:</strong>
              <%= product.quantity %>
            </p>
            <p>
              <strong>Код:</strong>
              <%= product.product_code %>
            </p>
            <p><%= social_share_button_tag(product.title) %></p>
            <%= button_to product_line_items_path(product.id), class: "btn btn-danger" do %>
              <%= fa_icon 'cart-plus', text: 'В Корзину' %>
            <% end %>
          </div>
        </div>
        <div class="row">
          <div class="col-md-12 col-xs-12" id="slider-thumbs">
            <ul class="hide-bullets">
              <li class="col-sm-3 col-xs-6">
                <a class="thumbnail product_image_add" id="carousel-selector-0">
                  <%= image_tag(product.image_url(:medium), width: '150px', height: '150px') %>
                </a>
              </li>
              <% product.image_products.each_with_index do |im_prod, i| %>
                <li class="col-sm-3 col-xs-6">
                  <a class="thumbnail product_image_add" id="carousel-selector-<%= im_prod.id %>">
                    <%= image_tag(im_prod.image.url(:medium), width: '150px', height: '150px')%>
                  </a>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            <br>
            <button id="info-product_<%= i+1 %>" class='btn btn-primary btn-block'>Больше информации</button>
          </div>
        </div>
        <div class="row info-about-product_<%= i+1 %> info-about-product">
          <br>
          <div class="col-md-6">
            <%= render 'table_characters', product: product %>
          </div>
          <div class="col-md-6">
            <h2>Описание</h2>
            <%= raw product.description %>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <div class="row">
          <div class="col-md-12">
            <button type="button" class="btn btn-default" data-dismiss="modal">Закрыть</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<script>
  $(document).ready(function(){
    $("#info-product_<%= i+1 %>").click(function(){
      $(".info-about-product_<%= i+1 %>").toggle();
    });
  });
</script>
